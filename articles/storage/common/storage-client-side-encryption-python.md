---
title: "Szyfrowanie po stronie klienta z języka Python dla usługi Microsoft Azure Storage | Dokumentacja firmy Microsoft"
description: "Biblioteka klienta magazynu Azure dla języka Python obsługuje szyfrowanie po stronie klienta dla zapewnienia maksymalnego poziomu bezpieczeństwa dla aplikacji usługi Azure Storage."
services: storage
documentationcenter: python
author: lakasa
manager: jahogg
editor: tysonn
ms.assetid: f9bf7981-9948-4f83-8931-b15679a09b8a
ms.service: storage
ms.workload: storage
ms.tgt_pltfrm: na
ms.devlang: python
ms.topic: article
ms.date: 05/11/2017
ms.author: lakasa
ms.openlocfilehash: 25a376b2e54953602b66abc3bae878f09a776a80
ms.sourcegitcommit: 18ad9bc049589c8e44ed277f8f43dcaa483f3339
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 08/29/2017
---
# <a name="client-side-encryption-with-python-for-microsoft-azure-storage"></a><span data-ttu-id="50440-103">Szyfrowanie po stronie klienta z języka Python dla magazynu Microsoft Azure</span><span class="sxs-lookup"><span data-stu-id="50440-103">Client-Side Encryption with Python for Microsoft Azure Storage</span></span>
[!INCLUDE [storage-selector-client-side-encryption-include](../../../includes/storage-selector-client-side-encryption-include.md)]

## <a name="overview"></a><span data-ttu-id="50440-104">Omówienie</span><span class="sxs-lookup"><span data-stu-id="50440-104">Overview</span></span>
<span data-ttu-id="50440-105">[Biblioteki klienta usługi Azure Storage dla języka Python](https://pypi.python.org/pypi/azure-storage) obsługuje szyfrowanie danych w aplikacjach klienckich przed przekazaniem do usługi Azure Storage i odszyfrowywania danych podczas pobierania do klienta.</span><span class="sxs-lookup"><span data-stu-id="50440-105">The [Azure Storage Client Library for Python](https://pypi.python.org/pypi/azure-storage) supports encrypting data within client applications before uploading to Azure Storage, and decrypting data while downloading to the client.</span></span>

> [!NOTE]
> <span data-ttu-id="50440-106">Biblioteka języka Python magazynu Azure znajduje się w wersji zapoznawczej.</span><span class="sxs-lookup"><span data-stu-id="50440-106">The Azure Storage Python library is in preview.</span></span>
> 
> 

## <a name="encryption-and-decryption-via-the-envelope-technique"></a><span data-ttu-id="50440-107">Szyfrowanie i odszyfrowywanie za pomocą techniki koperty</span><span class="sxs-lookup"><span data-stu-id="50440-107">Encryption and decryption via the envelope technique</span></span>
<span data-ttu-id="50440-108">Procesy szyfrowania i odszyfrowywania wykonaj technika koperty.</span><span class="sxs-lookup"><span data-stu-id="50440-108">The processes of encryption and decryption follow the envelope technique.</span></span>

### <a name="encryption-via-the-envelope-technique"></a><span data-ttu-id="50440-109">Szyfrowanie za pomocą techniki koperty</span><span class="sxs-lookup"><span data-stu-id="50440-109">Encryption via the envelope technique</span></span>
<span data-ttu-id="50440-110">Szyfrowanie za pomocą techniki koperty działa w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="50440-110">Encryption via the envelope technique works in the following way:</span></span>

1. <span data-ttu-id="50440-111">Biblioteka klienta magazynu Azure generuje klucz szyfrowania zawartości (CEK), jest jeden jednorazowej użycia klucza symetrycznego.</span><span class="sxs-lookup"><span data-stu-id="50440-111">The Azure storage client library generates a content encryption key (CEK), which is a one-time-use symmetric key.</span></span>
2. <span data-ttu-id="50440-112">Dane użytkownika są szyfrowane przy użyciu tego CEK.</span><span class="sxs-lookup"><span data-stu-id="50440-112">User data is encrypted using this CEK.</span></span>
3. <span data-ttu-id="50440-113">CEK jest następnie opakowane (zaszyfrowane) przy użyciu klucza szyfrowania klucza (KEK).</span><span class="sxs-lookup"><span data-stu-id="50440-113">The CEK is then wrapped (encrypted) using the key encryption key (KEK).</span></span> <span data-ttu-id="50440-114">KEK jest identyfikowany przez identyfikator klucza i może być parę klucza asymetrycznego lub klucza symetrycznego, które jest zarządzane lokalnie.</span><span class="sxs-lookup"><span data-stu-id="50440-114">The KEK is identified by a key identifier and can be an asymmetric key pair or a symmetric key, which is managed locally.</span></span>
   <span data-ttu-id="50440-115">Biblioteka klienta magazynu się nigdy nie ma dostęp do KEK.</span><span class="sxs-lookup"><span data-stu-id="50440-115">The storage client library itself never has access to KEK.</span></span> <span data-ttu-id="50440-116">Biblioteka wywołuje Algorytm zawijania klucza dostarczonego przez KEK.</span><span class="sxs-lookup"><span data-stu-id="50440-116">The library invokes the key wrapping algorithm that is provided by the KEK.</span></span> <span data-ttu-id="50440-117">Użytkownicy mogą wybierać dostawców niestandardowych dla klucza zawijania/odkodowywania w razie potrzeby.</span><span class="sxs-lookup"><span data-stu-id="50440-117">Users can choose to use custom providers for key wrapping/unwrapping if desired.</span></span>
4. <span data-ttu-id="50440-118">Zaszyfrowane dane są następnie przekazywane do usługi Magazyn Azure.</span><span class="sxs-lookup"><span data-stu-id="50440-118">The encrypted data is then uploaded to the Azure Storage service.</span></span> <span data-ttu-id="50440-119">Opakowana klucz wraz ze niektóre metadane dodatkowego szyfrowania jest przechowywane jako metadanych (dla obiektu blob) albo interpolowane z zaszyfrowanych danych (wiadomości w kolejce i jednostek tabeli).</span><span class="sxs-lookup"><span data-stu-id="50440-119">The wrapped key along with some additional encryption metadata is either stored as metadata (on a blob) or interpolated with the encrypted data (queue messages and table entities).</span></span>

### <a name="decryption-via-the-envelope-technique"></a><span data-ttu-id="50440-120">Odszyfrowywanie przy użyciu techniki koperty</span><span class="sxs-lookup"><span data-stu-id="50440-120">Decryption via the envelope technique</span></span>
<span data-ttu-id="50440-121">Odszyfrowywanie przy użyciu techniki koperty działa w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="50440-121">Decryption via the envelope technique works in the following way:</span></span>

1. <span data-ttu-id="50440-122">Biblioteka klienta przyjęto założenie, że użytkownik zarządza klucz szyfrowania klucza (KEK) lokalnie.</span><span class="sxs-lookup"><span data-stu-id="50440-122">The client library assumes that the user is managing the key encryption key (KEK) locally.</span></span> <span data-ttu-id="50440-123">Użytkownik nie musi wiedzieć określonego klucza, który był używany do szyfrowania.</span><span class="sxs-lookup"><span data-stu-id="50440-123">The user does not need to know the specific key that was used for encryption.</span></span> <span data-ttu-id="50440-124">Zamiast tego klucza rozpoznawania nazw, który jest rozpoznawany jako różne identyfikatory klucza klucze, można skonfigurować i używane.</span><span class="sxs-lookup"><span data-stu-id="50440-124">Instead, a key resolver, which resolves different key identifiers to keys, can be set up and used.</span></span>
2. <span data-ttu-id="50440-125">Biblioteka klienta pobierze zaszyfrowanych danych wraz z materiał szyfrowania, który jest przechowywany w usłudze.</span><span class="sxs-lookup"><span data-stu-id="50440-125">The client library downloads the encrypted data along with any encryption material that is stored on the service.</span></span>
3. <span data-ttu-id="50440-126">Klucz szyfrowania zawartości opakowana (CEK) znajduje się bez otoki (odszyfrowane) przy użyciu klucza szyfrowania klucza (KEK).</span><span class="sxs-lookup"><span data-stu-id="50440-126">The wrapped content encryption key (CEK) is then unwrapped (decrypted) using the key encryption key (KEK).</span></span> <span data-ttu-id="50440-127">W tym miejscu ponownie, Biblioteka klienta nie ma dostępu do KEK.</span><span class="sxs-lookup"><span data-stu-id="50440-127">Here again, the client library does not have access to KEK.</span></span> <span data-ttu-id="50440-128">Wywołuje po prostu niestandardowego dostawcy odszyfrowania algorytmu.</span><span class="sxs-lookup"><span data-stu-id="50440-128">It simply invokes the custom provider's unwrapping algorithm.</span></span>
4. <span data-ttu-id="50440-129">Klucz szyfrowania zawartości (CEK) jest następnie używany do odszyfrowywania danych zaszyfrowanych użytkownika.</span><span class="sxs-lookup"><span data-stu-id="50440-129">The content encryption key (CEK) is then used to decrypt the encrypted user data.</span></span>

## <a name="encryption-mechanism"></a><span data-ttu-id="50440-130">Mechanizm szyfrowania</span><span class="sxs-lookup"><span data-stu-id="50440-130">Encryption Mechanism</span></span>
<span data-ttu-id="50440-131">Biblioteka klienta magazynu używa [AES](http://en.wikipedia.org/wiki/Advanced_Encryption_Standard) w celu zaszyfrowania danych użytkownika.</span><span class="sxs-lookup"><span data-stu-id="50440-131">The storage client library uses [AES](http://en.wikipedia.org/wiki/Advanced_Encryption_Standard) in order to encrypt user data.</span></span> <span data-ttu-id="50440-132">W szczególności [Cipher Block Chaining (CBC)](http://en.wikipedia.org/wiki/Block_cipher_mode_of_operation#Cipher-block_chaining_.28CBC.29) trybu z użyciem standardu AES.</span><span class="sxs-lookup"><span data-stu-id="50440-132">Specifically, [Cipher Block Chaining (CBC)](http://en.wikipedia.org/wiki/Block_cipher_mode_of_operation#Cipher-block_chaining_.28CBC.29) mode with AES.</span></span> <span data-ttu-id="50440-133">Każdy usługi działa nieco inaczej, dlatego omówimy każdego z nich w tym miejscu.</span><span class="sxs-lookup"><span data-stu-id="50440-133">Each service works somewhat differently, so we will discuss each of them here.</span></span>

### <a name="blobs"></a><span data-ttu-id="50440-134">Obiekty blob</span><span class="sxs-lookup"><span data-stu-id="50440-134">Blobs</span></span>
<span data-ttu-id="50440-135">Biblioteka klienta obecnie obsługuje szyfrowanie całego obiektów blob tylko.</span><span class="sxs-lookup"><span data-stu-id="50440-135">The client library currently supports encryption of whole blobs only.</span></span> <span data-ttu-id="50440-136">W szczególności, szyfrowanie jest obsługiwane, gdy użytkownicy używają **utworzyć*** metody.</span><span class="sxs-lookup"><span data-stu-id="50440-136">Specifically, encryption is supported when users use the **create*** methods.</span></span> <span data-ttu-id="50440-137">Obsługiwane są pliki do pobrania, zarówno pełne i pobieranie zakresu, i paralelizacja przekazywania i pobierania jest dostępna.</span><span class="sxs-lookup"><span data-stu-id="50440-137">For downloads, both complete and range downloads are supported, and parallelization of both upload and download is available.</span></span>

<span data-ttu-id="50440-138">Podczas szyfrowania biblioteki klienckiej generowania losowego inicjowania wektora (IV) 16 bajtów oraz klucz szyfrowania zawartości (CEK) 32 bajtów i wykonać koperty szyfrowanie danych obiektów blob, korzystając z tych informacji.</span><span class="sxs-lookup"><span data-stu-id="50440-138">During encryption, the client library will generate a random Initialization Vector (IV) of 16 bytes, together with a random content encryption key (CEK) of 32 bytes, and perform envelope encryption of the blob data using this information.</span></span> <span data-ttu-id="50440-139">Opakowana CEK i niektóre metadane szyfrowania dodatkowe następnie są przechowywane jako obiektów blob metadane wraz z zaszyfrowanego obiektu blob w usłudze.</span><span class="sxs-lookup"><span data-stu-id="50440-139">The wrapped CEK and some additional encryption metadata are then stored as blob metadata along with the encrypted blob on the service.</span></span>

> [!WARNING]
> <span data-ttu-id="50440-140">W przypadku edytowania lub przekazywanie metadanych dla obiektu blob, należy upewnij się, że te metadane są zachowywane.</span><span class="sxs-lookup"><span data-stu-id="50440-140">If you are editing or uploading your own metadata for the blob, you need to ensure that this metadata is preserved.</span></span> <span data-ttu-id="50440-141">Po wysłaniu nowej metadanych bez takich metadanych opakowana CEK, IV i inne metadane zostaną utracone i zawartości obiektu blob nigdy nie być pobieranie.</span><span class="sxs-lookup"><span data-stu-id="50440-141">If you upload new metadata without this metadata, the wrapped CEK, IV and other metadata will be lost and the blob content will never be retrievable again.</span></span>
> 
> 

<span data-ttu-id="50440-142">Pobieranie zaszyfrowanej blob polega na pobraniu zawartości przy użyciu całego obiektu blob **uzyskać*** podręczne metody.</span><span class="sxs-lookup"><span data-stu-id="50440-142">Downloading an encrypted blob involves retrieving the content of the entire blob using the **get*** convenience methods.</span></span> <span data-ttu-id="50440-143">Opakowana CEK następnie rozpakować i używać razem z IV (przechowywane jako metadane obiektu blob w tym przypadku), aby przywrócić odszyfrowane dane użytkowników.</span><span class="sxs-lookup"><span data-stu-id="50440-143">The wrapped CEK is unwrapped and used together with the IV (stored as blob metadata in this case) to return the decrypted data to the users.</span></span>

<span data-ttu-id="50440-144">Pobieranie z dowolnego zakresu (**uzyskać*** przekazano metody z parametrami zakresu) w obiekcie blob zaszyfrowane obejmuje Dopasowywanie zakresu dostarczone przez użytkowników w celu pobrania niewielkie dodatkowych danych, który może służyć do odszyfrowywania pomyślnie żądany zakres.</span><span class="sxs-lookup"><span data-stu-id="50440-144">Downloading an arbitrary range (**get*** methods with range parameters passed in) in the encrypted blob involves adjusting the range provided by users in order to get a small amount of additional data that can be used to successfully decrypt the requested range.</span></span>

<span data-ttu-id="50440-145">Blokowe i stronicowe obiekty BLOB tylko może być szyfrowane/odszyfrować za pomocą tego systemu.</span><span class="sxs-lookup"><span data-stu-id="50440-145">Block blobs and page blobs only can be encrypted/decrypted using this scheme.</span></span> <span data-ttu-id="50440-146">Obecnie nie jest obsługiwane dla szyfrowania uzupełnialnych obiektów blob.</span><span class="sxs-lookup"><span data-stu-id="50440-146">There is currently no support for encrypting append blobs.</span></span>

### <a name="queues"></a><span data-ttu-id="50440-147">Kolejki</span><span class="sxs-lookup"><span data-stu-id="50440-147">Queues</span></span>
<span data-ttu-id="50440-148">Ponieważ wiadomości w kolejce może być dowolnym formacie, Biblioteka klienta definiuje formatu niestandardowego, który obejmuje wektor inicjowania (IV) oraz klucza zaszyfrowanego szyfrowania zawartości (CEK) w treści wiadomości.</span><span class="sxs-lookup"><span data-stu-id="50440-148">Since queue messages can be of any format, the client library defines a custom format that includes the Initialization Vector (IV) and the encrypted content encryption key (CEK) in the message text.</span></span>

<span data-ttu-id="50440-149">Podczas szyfrowania biblioteki klienckiej generuje losowe IV 16 bajtów wraz z losowe CEK 32 bajtów i wykonuje koperty szyfrowania treści wiadomości kolejki, korzystając z tych informacji.</span><span class="sxs-lookup"><span data-stu-id="50440-149">During encryption, the client library generates a random IV of 16 bytes along with a random CEK of 32 bytes and performs envelope encryption of the queue message text using this information.</span></span> <span data-ttu-id="50440-150">Opakowana CEK i niektóre metadane dodatkowego szyfrowania jest następnie dodawana do komunikatu w kolejce zaszyfrowane.</span><span class="sxs-lookup"><span data-stu-id="50440-150">The wrapped CEK and some additional encryption metadata are then added to the encrypted queue message.</span></span> <span data-ttu-id="50440-151">Ten komunikat modyfikacji (pokazana poniżej) są przechowywane w usłudze.</span><span class="sxs-lookup"><span data-stu-id="50440-151">This modified message (shown below) is stored on the service.</span></span>

```
<MessageText>{"EncryptedMessageContents":"6kOu8Rq1C3+M1QO4alKLmWthWXSmHV3mEfxBAgP9QGTU++MKn2uPq3t2UjF1DO6w","EncryptionData":{…}}</MessageText>
```

<span data-ttu-id="50440-152">Podczas odszyfrowywania opakowana klucz zostaje wyodrębniony z komunikatu w kolejce i nie opakowanych.</span><span class="sxs-lookup"><span data-stu-id="50440-152">During decryption, the wrapped key is extracted from the queue message and unwrapped.</span></span> <span data-ttu-id="50440-153">IV jest również wyodrębniony z komunikatu w kolejce i używać razem z kluczem odkodowany do odszyfrowania danych kolejki wiadomości.</span><span class="sxs-lookup"><span data-stu-id="50440-153">The IV is also extracted from the queue message and used along with the unwrapped key to decrypt the queue message data.</span></span> <span data-ttu-id="50440-154">Należy pamiętać, że metadane szyfrowania mały (w obszarze 500 bajtów), więc podczas jego wliczane limit 64KB komunikatu w kolejce, wpływ powinno być łatwe w zarządzaniu.</span><span class="sxs-lookup"><span data-stu-id="50440-154">Note that the encryption metadata is small (under 500 bytes), so while it does count toward the 64KB limit for a queue message, the impact should be manageable.</span></span>

### <a name="tables"></a><span data-ttu-id="50440-155">Tabele</span><span class="sxs-lookup"><span data-stu-id="50440-155">Tables</span></span>
<span data-ttu-id="50440-156">Biblioteka klienta obsługuje szyfrowanie właściwości obiektów w operacjach wstawiania i zamienianie operacji.</span><span class="sxs-lookup"><span data-stu-id="50440-156">The client library supports encryption of entity properties for insert and replace operations.</span></span>

> [!NOTE]
> <span data-ttu-id="50440-157">Scalanie nie jest obecnie obsługiwane.</span><span class="sxs-lookup"><span data-stu-id="50440-157">Merge is not currently supported.</span></span> <span data-ttu-id="50440-158">Ponieważ podzbiór właściwości może być zaszyfrowany wcześniej za pomocą innego klucza, po prostu scalanie nowych właściwości i aktualizowania metadanych spowoduje utratę danych.</span><span class="sxs-lookup"><span data-stu-id="50440-158">Since a subset of properties may have been encrypted previously using a different key, simply merging the new properties and updating the metadata will result in data loss.</span></span> <span data-ttu-id="50440-159">Scalanie albo wymaga wywołań usługi dodatkowe odczytać istniejące jednostki z usługi lub przy użyciu nowego klucza dla właściwości, które nie są odpowiednie ze względu na wydajność.</span><span class="sxs-lookup"><span data-stu-id="50440-159">Merging either requires making extra service calls to read the pre-existing entity from the service, or using a new key per property, both of which are not suitable for performance reasons.</span></span>
> 
> 

<span data-ttu-id="50440-160">Szyfrowanie danych tabeli działa w następujący sposób:</span><span class="sxs-lookup"><span data-stu-id="50440-160">Table data encryption works as follows:</span></span>

1. <span data-ttu-id="50440-161">Użytkownicy określić właściwości, które mają być szyfrowane.</span><span class="sxs-lookup"><span data-stu-id="50440-161">Users specify the properties to be encrypted.</span></span>
2. <span data-ttu-id="50440-162">Biblioteka klienta generuje losowe inicjowania wektora (IV) 16 bajtów oraz klucza szyfrowania zawartości (CEK) 32 bajtów dla każdej jednostki i wykonuje szyfrowanie koperty na poszczególnych właściwości w celu zaszyfrowania przez wyprowadzanie nowy IV dla właściwości.</span><span class="sxs-lookup"><span data-stu-id="50440-162">The client library generates a random Initialization Vector (IV) of 16 bytes along with a random content encryption key (CEK) of 32 bytes for every entity, and performs envelope encryption on the individual properties to be encrypted by deriving a new IV per property.</span></span> <span data-ttu-id="50440-163">Zaszyfrowane właściwości są przechowywane jako dane binarne.</span><span class="sxs-lookup"><span data-stu-id="50440-163">The encrypted property is stored as binary data.</span></span>
3. <span data-ttu-id="50440-164">Opakowana CEK i niektóre metadane szyfrowania dodatkowe następnie są przechowywane jako dwa dodatkowe właściwości zastrzeżone.</span><span class="sxs-lookup"><span data-stu-id="50440-164">The wrapped CEK and some additional encryption metadata are then stored as two additional reserved properties.</span></span> <span data-ttu-id="50440-165">Właściwości zastrzeżone pierwszy (\_ClientEncryptionMetadata1) jest właściwością ciągu, która przechowuje informacje dotyczące IV, wersji i klucz opakowana.</span><span class="sxs-lookup"><span data-stu-id="50440-165">The first reserved property (\_ClientEncryptionMetadata1) is a string property that holds the information about IV, version, and wrapped key.</span></span> <span data-ttu-id="50440-166">Właściwości zastrzeżone drugiego (\_ClientEncryptionMetadata2) jest właściwość binarną, która przechowuje informacje o właściwościach, które są szyfrowane.</span><span class="sxs-lookup"><span data-stu-id="50440-166">The second reserved property (\_ClientEncryptionMetadata2) is a binary property that holds the information about the properties that are encrypted.</span></span> <span data-ttu-id="50440-167">Informacje przedstawione w tej drugiej właściwości (\_ClientEncryptionMetadata2) jest zaszyfrowany.</span><span class="sxs-lookup"><span data-stu-id="50440-167">The information in this second property (\_ClientEncryptionMetadata2) is itself encrypted.</span></span>
4. <span data-ttu-id="50440-168">Z powodu te dodatkowe właściwości zastrzeżone wymagana na potrzeby szyfrowania użytkownicy mogą teraz mieć tylko 250 właściwości niestandardowych zamiast 252.</span><span class="sxs-lookup"><span data-stu-id="50440-168">Due to these additional reserved properties required for encryption, users may now have only 250 custom properties instead of 252.</span></span> <span data-ttu-id="50440-169">Całkowity rozmiar jednostki musi być mniejszy niż 1MB.</span><span class="sxs-lookup"><span data-stu-id="50440-169">The total size of the entity must be less than 1MB.</span></span>
   
   <span data-ttu-id="50440-170">Należy pamiętać, że tylko właściwości parametrów mogą być szyfrowane.</span><span class="sxs-lookup"><span data-stu-id="50440-170">Note that only string properties can be encrypted.</span></span> <span data-ttu-id="50440-171">Inne typy właściwości mają być szyfrowane, musi zostać przekonwertowana na ciągi.</span><span class="sxs-lookup"><span data-stu-id="50440-171">If other types of properties are to be encrypted, they must be converted to strings.</span></span> <span data-ttu-id="50440-172">Zaszyfrowane ciągi są przechowywane w usłudze jako właściwości binarnych i są one konwertowana z powrotem do ciągi (nieprzetworzone ciągi, nie EntityProperties z typem EdmType.STRING) po odszyfrowywania.</span><span class="sxs-lookup"><span data-stu-id="50440-172">The encrypted strings are stored on the service as binary properties, and they are converted back to strings (raw strings, not EntityProperties with type EdmType.STRING) after decryption.</span></span>
   
   <span data-ttu-id="50440-173">W przypadku tabel, oprócz zasad szyfrowania użytkownicy muszą określić właściwości, które mają być szyfrowane.</span><span class="sxs-lookup"><span data-stu-id="50440-173">For tables, in addition to the encryption policy, users must specify the properties to be encrypted.</span></span> <span data-ttu-id="50440-174">To może odbywać się przez zapisanie tych właściwości w obiektach TableEntity z wartością typu EdmType.STRING i szyfrowania ma wartość true lub ustawienie encryption_resolver_function dla obiekt tableservice.</span><span class="sxs-lookup"><span data-stu-id="50440-174">This can be done by either storing these properties in TableEntity objects with the type set to EdmType.STRING and encrypt set to true or setting the encryption_resolver_function on the tableservice object.</span></span> <span data-ttu-id="50440-175">Rozwiązywanie problemów z szyfrowania to funkcja, która przyjmuje klucza partycji, klucz wiersza i nazwy właściwości i zwraca wartość boolean wskazującą, czy ma być szyfrowana tej właściwości.</span><span class="sxs-lookup"><span data-stu-id="50440-175">An encryption resolver is a function that takes a partition key, row key, and property name and returns a boolean that indicates whether that property should be encrypted.</span></span> <span data-ttu-id="50440-176">Podczas szyfrowania biblioteki klienckiej użyje tych informacji do określania, czy właściwości powinny być szyfrowane podczas zapisywania można przewodowo.</span><span class="sxs-lookup"><span data-stu-id="50440-176">During encryption, the client library will use this information to decide whether a property should be encrypted while writing to the wire.</span></span> <span data-ttu-id="50440-177">Delegat także możliwość logiki wokół jak właściwości są szyfrowane.</span><span class="sxs-lookup"><span data-stu-id="50440-177">The delegate also provides for the possibility of logic around how properties are encrypted.</span></span> <span data-ttu-id="50440-178">(Na przykład, jeśli X, następnie szyfrować właściwości A; w przeciwnym razie szyfrowania właściwości A i B.) Należy pamiętać, że nie jest niezbędne do zapewnienia tych informacji podczas odczytywania lub zapytanie jednostki.</span><span class="sxs-lookup"><span data-stu-id="50440-178">(For example, if X, then encrypt property A; otherwise encrypt properties A and B.) Note that it is not necessary to provide this information while reading or querying entities.</span></span>

### <a name="batch-operations"></a><span data-ttu-id="50440-179">Operacje w trybie wsadowym</span><span class="sxs-lookup"><span data-stu-id="50440-179">Batch Operations</span></span>
<span data-ttu-id="50440-180">Jedne zasady szyfrowania ma zastosowanie do wszystkich wierszy w partii.</span><span class="sxs-lookup"><span data-stu-id="50440-180">One encryption policy applies to all rows in the batch.</span></span> <span data-ttu-id="50440-181">Biblioteka klienta wewnętrznie wygeneruje nowy losowych IV i losowe CEK wierszu w partii.</span><span class="sxs-lookup"><span data-stu-id="50440-181">The client library will internally generate a new random IV and random CEK per row in the batch.</span></span> <span data-ttu-id="50440-182">Użytkowników można również zaszyfrować inne właściwości dla każdej operacji w partii, definiując to zachowanie w szyfrowania programu rozpoznawania nazw.</span><span class="sxs-lookup"><span data-stu-id="50440-182">Users can also choose to encrypt different properties for every operation in the batch by defining this behavior in the encryption resolver.</span></span>
<span data-ttu-id="50440-183">Jeśli plik wsadowy jest tworzony jako menedżera kontekstu za pomocą metody batch() tableservice, zasady szyfrowania tableservice zostaną automatycznie zastosowane do wykonywania zadania wsadowego.</span><span class="sxs-lookup"><span data-stu-id="50440-183">If a batch is created as a context manager through the tableservice batch() method, the tableservice's encryption policy will automatically be applied to the batch.</span></span> <span data-ttu-id="50440-184">Plik wsadowy jest jawnie utworzone przez wywołanie konstruktora, zasady szyfrowania należy przekazać jako parametru i lewej nie mają być modyfikowane przez czas ich istnienia partii.</span><span class="sxs-lookup"><span data-stu-id="50440-184">If a batch is created explicitly by calling the constructor, the encryption policy must be passed as a parameter and left unmodified for the lifetime of the batch.</span></span>
<span data-ttu-id="50440-185">Należy pamiętać, że jednostki są szyfrowane, ponieważ są one wstawiane do partii za pomocą zasad szyfrowania wykonywania zadania wsadowego (jednostek nie są szyfrowane w czasie zatwierdzania partii za pomocą zasad szyfrowania tableservice).</span><span class="sxs-lookup"><span data-stu-id="50440-185">Note that entities are encrypted as they are inserted into the batch using the batch's encryption policy (entities are NOT encrypted at the time of committing the batch using the tableservice's encryption policy).</span></span>

### <a name="queries"></a><span data-ttu-id="50440-186">Zapytania</span><span class="sxs-lookup"><span data-stu-id="50440-186">Queries</span></span>
<span data-ttu-id="50440-187">Aby wykonać operacje zapytań, należy określić klucza programu rozpoznawania nazw, która jest w stanie rozwiązać wszystkich kluczy w zestawie wyników.</span><span class="sxs-lookup"><span data-stu-id="50440-187">To perform query operations, you must specify a key resolver that is able to resolve all the keys in the result set.</span></span> <span data-ttu-id="50440-188">Jeśli nie można rozpoznać jednostki zawarty w wyniku zapytania do dostawcy, biblioteki klienckiej zgłosi błąd.</span><span class="sxs-lookup"><span data-stu-id="50440-188">If an entity contained in the query result cannot be resolved to a provider, the client library will throw an error.</span></span> <span data-ttu-id="50440-189">Dla dowolnego zapytania, który wykonuje projekcje po stronie serwera, Biblioteka klienta spowoduje dodanie właściwości metadanych szyfrowania specjalne (\_ClientEncryptionMetadata1 i \_ClientEncryptionMetadata2), aby domyślnie wybranych kolumn.</span><span class="sxs-lookup"><span data-stu-id="50440-189">For any query that performs server side projections, the client library will add the special encryption metadata properties (\_ClientEncryptionMetadata1 and \_ClientEncryptionMetadata2) by default to the selected columns.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="50440-190">Należy pamiętać o tych punktów ważne przy użyciu szyfrowania po stronie klienta:</span><span class="sxs-lookup"><span data-stu-id="50440-190">Be aware of these important points when using client-side encryption:</span></span>
> 
> * <span data-ttu-id="50440-191">Gdy odczyt lub zapis do obiektu blob zaszyfrowane, użyj całego obiektu blob przekazywania oraz poleceń pobieranie obiektu blob zakresu/całości.</span><span class="sxs-lookup"><span data-stu-id="50440-191">When reading from or writing to an encrypted blob, use whole blob upload commands and range/whole blob download commands.</span></span> <span data-ttu-id="50440-192">Należy unikać pisania do obiektu blob zaszyfrowane za pomocą protokołu operacji, takich jak Put bloku, umieść listy zablokowanych, zapisu stron lub wyczyść stron. w przeciwnym razie możesz uszkodzony zaszyfrowany obiekt blob i stał się niemożliwe do odczytania.</span><span class="sxs-lookup"><span data-stu-id="50440-192">Avoid writing to an encrypted blob using protocol operations such as Put Block, Put Block List, Write Pages, or Clear Pages; otherwise you may corrupt the encrypted blob and make it unreadable.</span></span>
> * <span data-ttu-id="50440-193">W przypadku tabel istnieje ograniczenie podobne.</span><span class="sxs-lookup"><span data-stu-id="50440-193">For tables, a similar constraint exists.</span></span> <span data-ttu-id="50440-194">Należy zachować ostrożność nie aktualizować zaszyfrowane właściwości bez aktualizowania metadanych szyfrowania.</span><span class="sxs-lookup"><span data-stu-id="50440-194">Be careful to not update encrypted properties without updating the encryption metadata.</span></span>
> * <span data-ttu-id="50440-195">Jeśli ustawisz metadanych w obiekcie blob zaszyfrowane, mogą zastąpić metadane związane z szyfrowaniem wymaganych do odszyfrowania, ponieważ ustawienie metadanych nie jest dodatek.</span><span class="sxs-lookup"><span data-stu-id="50440-195">If you set metadata on the encrypted blob, you may overwrite the encryption-related metadata required for decryption, since setting metadata is not additive.</span></span> <span data-ttu-id="50440-196">Dotyczy to również migawki; Unikaj określania metadanych podczas tworzenia migawki obiektu blob zaszyfrowane.</span><span class="sxs-lookup"><span data-stu-id="50440-196">This is also true for snapshots; avoid specifying metadata while creating a snapshot of an encrypted blob.</span></span> <span data-ttu-id="50440-197">Jeśli metadane muszą być ustawione, należy wywołać **get_blob_metadata** metodę najpierw uzyskać bieżące metadane szyfrowania i uniknąć równoczesnych zapisów podczas ustawiania metadanych.</span><span class="sxs-lookup"><span data-stu-id="50440-197">If metadata must be set, be sure to call the **get_blob_metadata** method first to get the current encryption metadata, and avoid concurrent writes while metadata is being set.</span></span>
> * <span data-ttu-id="50440-198">Włącz **require_encryption** flagi na obiekt usługi dla użytkowników, którzy powinien działać wyłącznie z zaszyfrowanych danych.</span><span class="sxs-lookup"><span data-stu-id="50440-198">Enable the **require_encryption** flag on the service object for users that should work only with encrypted data.</span></span> <span data-ttu-id="50440-199">Aby uzyskać więcej informacji, zobacz poniżej.</span><span class="sxs-lookup"><span data-stu-id="50440-199">See below for more info.</span></span>
> 
> 

<span data-ttu-id="50440-200">Biblioteka klienta magazynu oczekuje podana KEK i rozpoznawania klucza do implementacji interfejsu.</span><span class="sxs-lookup"><span data-stu-id="50440-200">The storage client library expects the provided KEK and key resolver to implement the following interface.</span></span> <span data-ttu-id="50440-201">[Usługa Azure Key Vault](https://azure.microsoft.com/services/key-vault/) obsługę zarządzania Python KEK oczekuje i będzie można zintegrować z tej biblioteki, po zakończeniu.</span><span class="sxs-lookup"><span data-stu-id="50440-201">[Azure Key Vault](https://azure.microsoft.com/services/key-vault/) support for Python KEK management is pending and will be integrated into this library when completed.</span></span>

## <a name="client-api--interface"></a><span data-ttu-id="50440-202">Interfejs API klienta / interfejsu</span><span class="sxs-lookup"><span data-stu-id="50440-202">Client API / Interface</span></span>
<span data-ttu-id="50440-203">Po utworzeniu obiektu usługi Magazyn (tj. blockblobservice), użytkownik może przypisać wartości do pola, które tworzą zasadę szyfrowania: key_encryption_key, key_resolver_function i require_encryption.</span><span class="sxs-lookup"><span data-stu-id="50440-203">After a storage service object (i.e. blockblobservice) has been created, the user may assign values to the fields that constitute an encryption policy: key_encryption_key, key_resolver_function, and require_encryption.</span></span> <span data-ttu-id="50440-204">Użytkownicy mogą podać tylko KEK tylko program rozpoznawania nazw, lub obie.</span><span class="sxs-lookup"><span data-stu-id="50440-204">Users can provide only a KEK, only a resolver, or both.</span></span> <span data-ttu-id="50440-205">key_encryption_key jest podstawowy typ klucza, który jest identyfikowany przy użyciu identyfikatora klucza i zapewnia logiki zawijania/odkodowywania.</span><span class="sxs-lookup"><span data-stu-id="50440-205">key_encryption_key is the basic key type that is identified using a key identifier and that provides the logic for wrapping/unwrapping.</span></span> <span data-ttu-id="50440-206">key_resolver_function jest używany do rozpoznawania klucza podczas odszyfrowywania.</span><span class="sxs-lookup"><span data-stu-id="50440-206">key_resolver_function is used to resolve a key during the decryption process.</span></span> <span data-ttu-id="50440-207">Zwraca prawidłową KEK, podany identyfikator klucza.</span><span class="sxs-lookup"><span data-stu-id="50440-207">It returns a valid KEK given a key identifier.</span></span> <span data-ttu-id="50440-208">Zapewnia to użytkownikom wybór między wiele kluczy, które są zarządzane na wiele lokalizacji.</span><span class="sxs-lookup"><span data-stu-id="50440-208">This provides users the ability to choose between multiple keys that are managed in multiple locations.</span></span>

<span data-ttu-id="50440-209">KEK musi implementować następujące metody umożliwiające pomyślnie szyfrowania danych:</span><span class="sxs-lookup"><span data-stu-id="50440-209">The KEK must implement the following methods to successfully encrypt data:</span></span>

* <span data-ttu-id="50440-210">wrap_key(cek): Opakowuje CEK określonego (w bajtach) przy użyciu algorytmu wybrany przez użytkownika.</span><span class="sxs-lookup"><span data-stu-id="50440-210">wrap_key(cek): Wraps the specified CEK (bytes) using an algorithm of the user's choice.</span></span> <span data-ttu-id="50440-211">Zwraca klucz opakowana.</span><span class="sxs-lookup"><span data-stu-id="50440-211">Returns the wrapped key.</span></span>
* <span data-ttu-id="50440-212">get_key_wrap_algorithm(): zwraca algorytm stosowanego do opakowywania kluczy.</span><span class="sxs-lookup"><span data-stu-id="50440-212">get_key_wrap_algorithm(): Returns the algorithm used to wrap keys.</span></span>
* <span data-ttu-id="50440-213">get_kid(): zwraca identyfikator klucza w ciągu dla tego KEK.</span><span class="sxs-lookup"><span data-stu-id="50440-213">get_kid(): Returns the string key id for this KEK.</span></span>
  <span data-ttu-id="50440-214">KEK musi implementować następujących metod można pomyślnie odszyfrować danych:</span><span class="sxs-lookup"><span data-stu-id="50440-214">The KEK must implement the following methods to successfully decrypt data:</span></span>
* <span data-ttu-id="50440-215">unwrap_key (cek, algorytm): zwraca bez otoki formę CEK określony przy użyciu algorytmu określony ciąg.</span><span class="sxs-lookup"><span data-stu-id="50440-215">unwrap_key(cek, algorithm): Returns the unwrapped form of the specified CEK using the string-specified algorithm.</span></span>
* <span data-ttu-id="50440-216">get_kid(): zwraca ciąg identyfikatora klucza dla tego KEK.</span><span class="sxs-lookup"><span data-stu-id="50440-216">get_kid(): Returns a string key id for this KEK.</span></span>

<span data-ttu-id="50440-217">Program rozpoznawania nazw kluczy co najmniej musi implementować metodę, która podany identyfikator klucza, zwraca odpowiedniego klucza KEK implementującej interfejs powyżej.</span><span class="sxs-lookup"><span data-stu-id="50440-217">The key resolver must at least implement a method that, given a key id, returns the corresponding KEK implementing the interface above.</span></span> <span data-ttu-id="50440-218">Tylko ta metoda służy do przypisania do właściwości key_resolver_function na obiekt usługi.</span><span class="sxs-lookup"><span data-stu-id="50440-218">Only this method is to be assigned to the key_resolver_function property on the service object.</span></span>

* <span data-ttu-id="50440-219">Dla celów szyfrowania jest on używany zawsze i Brak klucza spowoduje błąd.</span><span class="sxs-lookup"><span data-stu-id="50440-219">For encryption, the key is used always and the absence of a key will result in an error.</span></span>
* <span data-ttu-id="50440-220">Do odszyfrowywania:</span><span class="sxs-lookup"><span data-stu-id="50440-220">For decryption:</span></span>
  
  * <span data-ttu-id="50440-221">Mechanizm rozpoznawania klucza jest wywoływana, jeśli określono, aby pobrać klucz.</span><span class="sxs-lookup"><span data-stu-id="50440-221">The key resolver is invoked if specified to get the key.</span></span> <span data-ttu-id="50440-222">Jeśli program rozpoznawania nazw jest określony, ale nie ma mapowania dla identyfikatora klucza, zostanie zgłoszony błąd.</span><span class="sxs-lookup"><span data-stu-id="50440-222">If the resolver is specified but does not have a mapping for the key identifier, an error is thrown.</span></span>
  * <span data-ttu-id="50440-223">Jeśli nie określono programu rozpoznawania nazw, ale nie określono klucza, klucz zostanie użyty, jeśli jego identyfikator odpowiada wymagany identyfikator klucza.</span><span class="sxs-lookup"><span data-stu-id="50440-223">If resolver is not specified but a key is specified, the key is used if its identifier matches the required key identifier.</span></span> <span data-ttu-id="50440-224">Jeśli identyfikator nie jest zgodne, zostanie zgłoszony błąd.</span><span class="sxs-lookup"><span data-stu-id="50440-224">If the identifier does not match, an error is thrown.</span></span>
    
    <span data-ttu-id="50440-225">Przykłady szyfrowania w azure.storage.samples <fix URL>zaprezentowania bardziej szczegółowe scenariusza end-to-end dla obiektów blob, kolejek i tabel.</span><span class="sxs-lookup"><span data-stu-id="50440-225">The encryption samples in azure.storage.samples <fix URL>demonstrate a more detailed end-to-end scenario for blobs, queues and tables.</span></span>
      <span data-ttu-id="50440-226">Przykładowe implementacje KEK i klucza program rozpoznawania nazw są zawarte w przykładowych plików jako KeyWrapper i KeyResolver odpowiednio.</span><span class="sxs-lookup"><span data-stu-id="50440-226">Sample implementations of the KEK and key resolver are provided in the sample files as KeyWrapper and KeyResolver respectively.</span></span>

### <a name="requireencryption-mode"></a><span data-ttu-id="50440-227">Tryb RequireEncryption</span><span class="sxs-lookup"><span data-stu-id="50440-227">RequireEncryption mode</span></span>
<span data-ttu-id="50440-228">Użytkownicy mogą opcjonalnie włączyć tryb działania, w której wszystkie przekazywania i pobierania muszą być szyfrowane.</span><span class="sxs-lookup"><span data-stu-id="50440-228">Users can optionally enable a mode of operation where all uploads and downloads must be encrypted.</span></span> <span data-ttu-id="50440-229">W tym trybie próby przekazania danych bez zasady szyfrowania lub pobrać dane, które nie są szyfrowane w usłudze zakończy się niepowodzeniem na kliencie.</span><span class="sxs-lookup"><span data-stu-id="50440-229">In this mode, attempts to upload data without an encryption policy or download data that is not encrypted on the service will fail on the client.</span></span> <span data-ttu-id="50440-230">**Require_encryption** flagi na obiekt usługi określa to zachowanie.</span><span class="sxs-lookup"><span data-stu-id="50440-230">The **require_encryption** flag on the service object controls this behavior.</span></span>

### <a name="blob-service-encryption"></a><span data-ttu-id="50440-231">Szyfrowanie usługi blob</span><span class="sxs-lookup"><span data-stu-id="50440-231">Blob service encryption</span></span>
<span data-ttu-id="50440-232">Ustawić szyfrowania pól zasad blockblobservice obiektu.</span><span class="sxs-lookup"><span data-stu-id="50440-232">Set the encryption policy fields on the blockblobservice object.</span></span> <span data-ttu-id="50440-233">Wszystkie inne będzie obsługiwany przez bibliotekę klienta wewnętrznie.</span><span class="sxs-lookup"><span data-stu-id="50440-233">Everything else will be handled by the client library internally.</span></span>

```python
# Create the KEK used for encryption.
# KeyWrapper is the provided sample implementation, but the user may use their own object as long as it implements the interface above.
kek = KeyWrapper('local:key1') # Key identifier

# Create the key resolver used for decryption.
# KeyResolver is the provided sample implementation, but the user may use whatever implementation they choose so long as the function set on the service object behaves appropriately.
key_resolver = KeyResolver()
key_resolver.put_key(kek)

# Set the KEK and key resolver on the service object.
my_block_blob_service.key_encryption_key = kek
my_block_blob_service.key_resolver_funcion = key_resolver.resolve_key

# Upload the encrypted contents to the blob.
my_block_blob_service.create_blob_from_stream(container_name, blob_name, stream)

# Download and decrypt the encrypted contents from the blob.
blob = my_block_blob_service.get_blob_to_bytes(container_name, blob_name)
```

### <a name="queue-service-encryption"></a><span data-ttu-id="50440-234">Szyfrowanie usługi kolejki</span><span class="sxs-lookup"><span data-stu-id="50440-234">Queue service encryption</span></span>
<span data-ttu-id="50440-235">Ustawić szyfrowania pól zasad queueservice obiektu.</span><span class="sxs-lookup"><span data-stu-id="50440-235">Set the encryption policy fields on the queueservice object.</span></span> <span data-ttu-id="50440-236">Wszystkie inne będzie obsługiwany przez bibliotekę klienta wewnętrznie.</span><span class="sxs-lookup"><span data-stu-id="50440-236">Everything else will be handled by the client library internally.</span></span>

```python
# Create the KEK used for encryption.
# KeyWrapper is the provided sample implementation, but the user may use their own object as long as it implements the interface above.
kek = KeyWrapper('local:key1') # Key identifier

# Create the key resolver used for decryption.
# KeyResolver is the provided sample implementation, but the user may use whatever implementation they choose so long as the function set on the service object behaves appropriately.
key_resolver = KeyResolver()
key_resolver.put_key(kek)

# Set the KEK and key resolver on the service object.
my_queue_service.key_encryption_key = kek
my_queue_service.key_resolver_funcion = key_resolver.resolve_key

# Add message
my_queue_service.put_message(queue_name, content)

# Retrieve message
retrieved_message_list = my_queue_service.get_messages(queue_name)
```

### <a name="table-service-encryption"></a><span data-ttu-id="50440-237">Szyfrowanie usługi tabel</span><span class="sxs-lookup"><span data-stu-id="50440-237">Table service encryption</span></span>
<span data-ttu-id="50440-238">Oprócz tworzenia zasady szyfrowania i ustawienie jej na żądanie opcji, należy określić **encryption_resolver_function** na **tableservice**, lub ustaw dla atrybutu Szyfruj EntityProperty.</span><span class="sxs-lookup"><span data-stu-id="50440-238">In addition to creating an encryption policy and setting it on request options, you must either specify an **encryption_resolver_function** on the **tableservice**, or set the encrypt attribute on the EntityProperty.</span></span>

### <a name="using-the-resolver"></a><span data-ttu-id="50440-239">Za pomocą programu rozpoznawania nazw</span><span class="sxs-lookup"><span data-stu-id="50440-239">Using the resolver</span></span>

```python
# Create the KEK used for encryption.
# KeyWrapper is the provided sample implementation, but the user may use their own object as long as it implements the interface above.
kek = KeyWrapper('local:key1') # Key identifier

# Create the key resolver used for decryption.
# KeyResolver is the provided sample implementation, but the user may use whatever implementation they choose so long as the function set on the service object behaves appropriately.
key_resolver = KeyResolver()
key_resolver.put_key(kek)

# Define the encryption resolver_function.
def my_encryption_resolver(pk, rk, property_name):
        if property_name == 'foo':
                return True
        return False

# Set the KEK and key resolver on the service object.
my_table_service.key_encryption_key = kek
my_table_service.key_resolver_funcion = key_resolver.resolve_key
my_table_service.encryption_resolver_function = my_encryption_resolver

# Insert Entity
my_table_service.insert_entity(table_name, entity)

# Retrieve Entity
# Note: No need to specify an encryption resolver for retrieve, but it is harmless to leave the property set.
my_table_service.get_entity(table_name, entity['PartitionKey'], entity['RowKey'])
```

### <a name="using-attributes"></a><span data-ttu-id="50440-240">Przy użyciu atrybutów</span><span class="sxs-lookup"><span data-stu-id="50440-240">Using attributes</span></span>
<span data-ttu-id="50440-241">Jak wspomniano powyżej, właściwość może być oznaczony do szyfrowania przez zapisanie jej w obiekcie EntityProperty i ustawienie pola szyfrowania.</span><span class="sxs-lookup"><span data-stu-id="50440-241">As mentioned above, a property may be marked for encryption by storing it in an EntityProperty object and setting the encrypt field.</span></span>

```python
encrypted_property_1 = EntityProperty(EdmType.STRING, value, encrypt=True)
```

## <a name="encryption-and-performance"></a><span data-ttu-id="50440-242">Szyfrowanie i wydajności</span><span class="sxs-lookup"><span data-stu-id="50440-242">Encryption and performance</span></span>
<span data-ttu-id="50440-243">Należy pamiętać, że szyfrowania z magazynu danych spowoduje zmniejszenie wydajności.</span><span class="sxs-lookup"><span data-stu-id="50440-243">Note that encrypting your storage data results in additional performance overhead.</span></span> <span data-ttu-id="50440-244">Musi zostać wygenerowany klucz zawartości i IV, muszą być szyfrowane samej zawartości i dodatkowe metadane muszą być sformatowane i przekazać.</span><span class="sxs-lookup"><span data-stu-id="50440-244">The content key and IV must be generated, the content itself must be encrypted, and additional metadata must be formatted and uploaded.</span></span> <span data-ttu-id="50440-245">Ten narzut będą się różnić w zależności od ilości danych szyfrowany.</span><span class="sxs-lookup"><span data-stu-id="50440-245">This overhead will vary depending on the quantity of data being encrypted.</span></span> <span data-ttu-id="50440-246">Zaleca się, że klienci zawsze testują wydajności w czasie projektowania.</span><span class="sxs-lookup"><span data-stu-id="50440-246">We recommend that customers always test their applications for performance during development.</span></span>

## <a name="next-steps"></a><span data-ttu-id="50440-247">Następne kroki</span><span class="sxs-lookup"><span data-stu-id="50440-247">Next steps</span></span>
* <span data-ttu-id="50440-248">Pobierz [biblioteki klienta usługi Azure Storage dla języka Java PyPi pakietu](https://pypi.python.org/pypi/azure-storage)</span><span class="sxs-lookup"><span data-stu-id="50440-248">Download the [Azure Storage Client Library for Java PyPi package](https://pypi.python.org/pypi/azure-storage)</span></span>
* <span data-ttu-id="50440-249">Pobierz [biblioteki klienta magazynu Azure dla języka Python źródła kodu z usługi GitHub](https://github.com/Azure/azure-storage-python)</span><span class="sxs-lookup"><span data-stu-id="50440-249">Download the [Azure Storage Client Library for Python Source Code from GitHub](https://github.com/Azure/azure-storage-python)</span></span>